# Baby cry classification — training, inference, and API
# On Linux, use the project venv (do NOT use system pip):
#   source venv/bin/activate && pip install -r requirements.txt
# Or: ./venv/bin/pip install -r requirements.txt
#   (venv\Scripts\activate on Windows)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tensorflow>=2.14
librosa>=0.10
matplotlib>=3.7
# API server
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
sounddevice>=0.4.6
